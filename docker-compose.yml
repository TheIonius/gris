services:
  gris:
    build: .
    container_name: gris-simulator
    ports:
      - "8080:8080"
    volumes:
      - gris-data:/app/data
    environment:
      - PORT=8080
      - GRIS_DB_PATH=/app/data/gris.db
      - GRIS_SIMULATION_THREADS=4
    restart: unless-stopped

volumes:
  gris-data:
